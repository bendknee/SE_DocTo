stages:
  - deploy

Deployment:
  image: ruby:latest
  stage: deploy
  type: deploy
  before_script:
    - gem install dpl
  script:
    - dpl --provider=heroku --app=$HEROKU_APP_NAME --api-key=$HEROKU_API_KEY
    - export HEROKU_API_KEY=$HEROKU_API_KEY
  when: manual
  only:
    - master